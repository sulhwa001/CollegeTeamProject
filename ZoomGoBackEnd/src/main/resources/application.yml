#spring.application.name=zooomgobackend
server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gosu
    username: root
    password: as1230
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true